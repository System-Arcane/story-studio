'use client';
import { useState } from 'react';
export default function Admin() { const [paused, setPaused] = useState(false); return <main className="admin"><a className="logo" href="/">STory<span>®</span></a><div className="work-heading"><span className="eyebrow muted">OPERATIONS / MOCKUP</span><h1>스튜디오 운영 현황</h1><p>모든 수치는 예시입니다. 실제 인증과 통계 수집은 연결되지 않았습니다.</p></div><div className="info-box">개발용 관리자 화면입니다. 현재 접근 제어가 없으므로 실제 운영 데이터나 개인정보를 넣지 마세요. 방문객 화면에는 관리자 링크를 제공하지 않습니다.</div><div className="process-grid">{[['체험 시작', '30'], ['제작 완료', '24'], ['공유 버튼 클릭', '12']].map(([label, value]) => <div className="process" key={label}><p>{label}</p><h1>{value}<small> 예시</small></h1></div>)}</div><button className="primary" onClick={() => setPaused(!paused)}>{paused ? '제작 재개 (목업)' : '제작 일시정지 (목업)'}</button><p role="status">목업 상태: {paused ? '일시정지' : '운영 중'} · 실제 제작에는 영향을 주지 않습니다.</p></main>; }
