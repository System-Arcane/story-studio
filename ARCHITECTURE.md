# STory 아키텍처

## 현재 구현
- `app/layout.tsx`: 메타데이터, 전역 CSS, 한국어 문서 루트.
- `app/page.tsx`: 클라이언트 상태로 랜딩·업로드·이야기 선택·진행·결과 화면 전환. 현재 단일 파일.
- `app/globals.css`: 디자인 토큰, 상품 일러스트, 반응형 스타일.
- `app/admin/page.tsx`: 인증 없는 개발용 예시 화면. 실제 관리자 기능이 아님.
- `localStorage`의 `story-result`: 마지막 목업 결과. 사진은 data URL로 브라우저에 보관.
- 외부 AI/API 호출 및 서버 저장 없음. Google Fonts 요청은 존재함.

## 다음 리팩터링의 목표 (아직 구현되지 않음)
`app/page.tsx`는 화면 조합, `components/story/`는 화면별 UI, `lib/story/`는 타입·목업 데이터·저장 데이터 검증을 담당하도록 분리한다. `lib/story/`는 UI를 import하지 않는다. 브라우저 저장소 접근은 클라이언트 경계에 둔다. 이 경계는 현재 자동 린트로 강제되지 않는다.

향후 생성 공급자 연결은 별도 요구 시 설계한다. 현재 UI에 API 키, 생성 SDK, 비밀 값을 넣지 않는다. 브라우저 저장 데이터와 파일 입력은 신뢰하지 않고 경계에서 검증한다.

## 병렬 수정
구조 분리 전에는 두 구현 에이전트에게 `app/page.tsx`를 함께 배정하지 않는다. 분석은 병렬 수행하고 수정은 한 담당자가 한다. 구조 분리 후에는 파일 단위 소유권을 정한 작업만 병렬로 수행한다. 패키지/lockfile/전역 CSS 변경은 메인이 조정한다.
